# ReadingTable

> **Design decision — 2026-08-08:** Before continuing the clock, calculator, weather, or music UI, read `DESIGN_REVIEW_LIVE_OBJECTS.md`. Adele and Kate have rejected the current generic white-tile presentation. The document contains the approved interaction direction, visual reset, British Table Favourites handoff, extraction-tool location, and pack business-model concept.
>
> **Superseding reset — 2026-08-08 (evening), full handover from Adele/Kate/Gigi:** all four utility objects (record player, clock, weather, calculator) reviewed against real photographed books/flowers/Polaroids and found flat/childish/clip-art beside them — **only Calendar's visual direction currently passes.** No further visual implementation until direction is re-approved next session. Full brief logged in the "Utility-object design reset" section below — **read it before touching Clock/Weather/Calculator/Music again.**

iOS app (SwiftUI + SwiftData) that lets you build a virtual coffee table styled with your books.

**Consumer-facing app name: MyTablo** (renamed from "Tablo" 2026-08-08). The GitHub repo, Xcode project file, and both target names are now also `MyTablo` (see below) — only the bundle identifier is still `com.adeleroberts.Tablo`, deliberately, to avoid creating another duplicate app install mid-testing and re-doing the WeatherKit Apple Developer Portal step; revisit that specific piece in its own dedicated pass right before App Store submission. Use "MyTablo" in any new user-facing text, docs, or launch materials.

**Full project rename completed 2026-08-08**, ahead of collaborating with other AIs/tools and before TestFlight:
- **GitHub repo** renamed by Adele via github.com → Settings (Claude's tool session has no GitHub credentials, same limitation as `git push`). Now `github.com/adelerobertsuk/MyTablo` — the old `ReadingTable` URL still redirects. Local `git remote origin` updated to match.
- **Xcode project renamed**: `ReadingTable.xcodeproj` → `MyTablo.xcodeproj`, both schemes (`MyTablo.xcscheme`, `MyTabloWidgetsExtension.xcscheme`), and both target `name`/`productName` fields (`ReadingTable` → `MyTablo`, `ReadingTableWidgetsExtension` → `MyTabloWidgetsExtension`) via precise, targeted edits to `project.pbxproj` (not a blanket find-replace — verified line-by-line via a Python script that asserted an exact match count before writing anything, so nothing silent got missed or over-matched). Also updated the widget extension's `CFBundleDisplayName` to "MyTablo Widgets" for consistency.
- **Deliberately left unchanged**: both `PRODUCT_BUNDLE_IDENTIFIER`s, the physical `ReadingTable/`/`ReadingTableWidgets/` source folders (still real directories on disk — this project uses Xcode's file-system-synchronized groups, so their `path` is load-bearing), both `.entitlements` files' filenames, `ReadingTableWidgets/Info.plist`'s path, and the widget's internal `kind: "ReadingTableWidgets"` string in `ReadingTableWidgets.swift` (renaming that risks breaking Adele's/Kate's already-placed home screen widgets, since WidgetKit tracks placed widgets by kind identifier).
- **Verified**: `rm -rf` on stale DerivedData, then a full `xcodebuild clean build` — succeeded and produced `MyTablo.app` with `MyTabloWidgetsExtension.appex` correctly embedded inside it. Confirmed via `PlistBuddy` that both bundle identifiers in the built app are byte-for-byte unchanged (`com.adeleroberts.Tablo` / `com.adeleroberts.Tablo.Widgets`).

## Working with Adele
- Adele is a **beginner** — explain concepts plainly, avoid unexplained jargon, and keep answers focused.
- Be token-efficient: read only what's needed, keep explanations tight.

## Architecture
- **Pattern:** MVVM. Views observe `ObservableObject` view models. SwiftData for persistence.
- **Concurrency:** prefer async/await. Avoid Combine (view models still import it but don't rely on it).
- `ReadingTableApp.swift` is the entry point — builds the `TabView` and injects view models. (`ContentView.swift` is unused default template.)

### Files
- `Models/Book.swift` — SwiftData `@Model`s: `Book`, `CoffeeTableComposition`, `ComposedBook`.
- `ViewModels/LibraryViewModel.swift` — add/delete/load books via ISBN.
- `ViewModels/CoffeeTableCompositionViewModel.swift` — manage table arrangements (add/move/remove/z-order books).
- `Services/BookMetadataProvider.swift` — protocol + `OpenLibraryMetadataService` (live) + `MockBookMetadataService` (test data).
- `Views/LibraryView.swift` — Library tab UI.
- `Views/CoffeeTableViewIntegrated.swift` — Table tab UI (drag/scale/rotate books, pick table surface).
- `Views/Widgets/SmallWidgetView.swift` — SwiftUI layout for the small home screen widget, shared into both the app and `ReadingTableWidgets` extension targets.
- `ReadingTableWidgets/` — the widget extension target (separate from the main app target). `ReadingTableWidgets.swift` has the `TimelineProvider` that reads the shared App Group SwiftData store.

## Features
- **Library tab:** enter ISBN → fetch title/author/cover from OpenLibrary → save. Grid of covers, delete via swipe or button.
- **Table tab:** choose a table surface image, add books from library, drag/rotate/scale them; autosaves on every change. Optional "Snap to Grid" toggle rounds drag positions to a 40pt grid; double-tap a book to reset its rotation to 0.

## Conventions
- PascalCase types, camelCase members; 4-space indent; no force-unwrapping.
- Use `DocumentationSearch` (xcode-tools MCP) for unfamiliar/new Apple APIs (Liquid Glass, FoundationModels, latest SwiftUI).
- Build with `BuildProject` (xcode-tools MCP), not command-line xcodebuild.
- Any change to a SwiftData `@Model` (adding/removing/renaming properties) must keep existing local records loadable — add new fields with defaults rather than making them required, so the persistent store doesn't crash on devices with existing data.

## Roadmap (logged 2026-08-07)
- [x] Default startup table: use `Kate-table-WhitePlaster` instead of `Kate-table-AntiqueWood`.
- [x] Z-order fix: newly added books/decorations should always land on top, not underneath existing items.
- [x] Top-of-table white gap fix: the six `Kate-table-*` surface photos have a soft transparent fade baked into their top ~19px; rendering now applies `.scaleEffect(1.04, anchor: .bottom)` before `.clipped()` to crop it out (TabloView.swift, CoffeeTableViewIntegrated.swift).
- [x] Library: allow editing a book card in place (title, author, custom cover) — tap any cover in the Library grid to open `EditBookView`. Fixes blank cards from failed ISBN lookups.
- [x] **Barcode scanner (VisionKit)** — `Views/BarcodeScannerView.swift`, wired into `AddBookView` via a "Scan Barcode" button that fills the ISBN field. Uses `DataScannerViewController` (EAN-13/EAN-8/UPC-E/Code128), checked with `.isSupported`/`.isAvailable` and shows a graceful fallback message when unavailable (confirmed working in Simulator, which has no camera). Requires `NSCameraUsageDescription` (added to project.pbxproj build settings). **Confirmed working on a real device 2026-08-07** — tested live on Kate's iPhone (paired over local network), scanned a real book barcode successfully.
- Device note: Adele's own iPhone previously would not establish a data connection over cable during testing (likely a charge-only cable or blocked port) — Kate's phone was used instead as a workaround. **Resolved 2026-08-08** — Adele's own iPhone now connects cleanly over cable and is available for on-device testing (camera, barcode scanner, etc.).
- [x] **Camera scan: photograph a physical book's front cover to use as custom cover artwork — built 2026-08-08.** First version used a plain `UIImagePickerController` photo (no crop/perspective correction) — Adele tested it on her real device, confirmed capture works end-to-end, but the raw photo included background/table around the book and wasn't cropped straight. Upgraded `Views/CameraCoverCapture.swift` to use VisionKit's `VNDocumentCameraViewController` instead (the same document-scanning tech behind Notes' "Scan Documents") — it auto-detects the book's edges live, applies perspective correction, and lets Adele adjust corners or retake before confirming, so the final image is a straight, cropped cover instead of a raw snapshot. Button relabelled "Scan Cover" in `LibraryView.swift`'s `CustomCoverPicker`. Scanned image still runs through the existing `normalizedOrientation()` + JPEG pipeline. Reuses the existing `NSCameraUsageDescription` — no new Info.plist entry needed.
  - **Follow-up 2026-08-08: three-way cover picker.** Adele asked for Scan, plain Photo, and Upload to all be available with icons, and whether that'd be cluttered as separate buttons. Agreed three side-by-side buttons would cramp/truncate at the picker's 120pt width — replaced with a single "Add Cover" button that opens a `Menu` with three icon-labeled rows: Scan Cover (`viewfinder`), Take Photo (`camera`), Choose from Library (`photo.on.rectangle`). Added `RawPhotoCaptureScreen` (plain `UIImagePickerController` camera, no crop) back into `Views/CameraCoverCapture.swift` alongside the scanner screen; gallery upload now triggered via the `.photosPicker(isPresented:...)` modifier instead of the inline `PhotosPicker` view, so all three fit the same Button-based menu row style. All three paths converge on the same `normalizedOrientation()` + JPEG pipeline.
  - **Bug found on real-device test 2026-08-08: Scan Cover saved the cover as black/inverted colors.** Take Photo and Choose from Library both looked correct — only Scan Cover was affected. Root cause: `normalizedOrientation()` (`LibraryView.swift`) skipped its redraw pass whenever `imageOrientation == .up`, to avoid unnecessary work. VisionKit's document scanner happens to always return `.up`-oriented images, but in a non-standard color space that renders inverted/black if saved untouched — so scanned covers were the one path that always skipped the redraw that would have fixed it. Every other source (raw camera, gallery) came back non-`.up` on this device and got redrawn anyway, so they looked fine. Fixed by always redrawing regardless of orientation, which forces a standard RGB color space on every cover.
  - **Investigated further 2026-08-08 — confirmed NOT a code bug.** Adele retested on a specific book ("Human Design Unlocked," a dark purple cover with metallic gold foil) and it was still black even after the fix. She then scanned the same physical book into Apple's own Notes app scanner and got the identical distorted result — ruling out our code. Root cause is inherent to `VNDocumentCameraViewController`/Notes' scanning tech: it's tuned for white paper documents and its auto-exposure can blow out or invert on dark, glossy, or metallic covers. No fix possible or needed on our side — Adele confirmed **Scan Cover works correctly on normal matte covers** (tested and confirmed working on "Outsmarting Reality," a plain black-and-white cover). Workaround for problem covers: use Take Photo or Choose from Library instead of Scan Cover.
  - **Follow-up 2026-08-08: background removal (subject lift) for cover photos — built, not yet visually confirmed.** Adele saw Kate use Apple Photos' "lift subject from background" on a photo of her red notebook to get a clean cutout, and asked if MyTablo could do the same — partly as a fallback for covers where Scan Cover's auto-exposure struggles (dark/glossy/metallic, per the bug above). Implemented using Vision's `VNGenerateForegroundInstanceMaskRequest` (the same subject-lift tech behind Photos), added to `Views/CameraCoverCapture.swift`. Take Photo and Choose from Library (not Scan Cover, which already crops via perspective correction) now route through a new `BackgroundRemovalReviewScreen`: shows the photo over a checkerboard so transparency is visible, with a "Remove Background" button to preview the cutout and a "Try Again" retry, before confirming with "Use Photo". Cutouts save as PNG (to keep transparency); plain photos still save as JPEG. Runs the Vision request off the main thread via `Task.detached`, keeping `UIImage` construction on the calling context to avoid a Swift 6 main-actor warning. Full project builds clean, 0 warnings. **Not yet visually confirmed** — this session's Simulator had the same synthetic-tap-on-`Button` limitation logged elsewhere in this file, blocking click-through testing of the picker menu; needs a real-device retest (also note Vision's subject detection, like the document scanner, may still struggle on some cover types — same caveat as Scan Cover).
  - **Bug found on real-device test 2026-08-08 — Take Photo (and Choose from Library) got stuck on a blank white screen after tapping "Use Photo"/finishing capture, blocking custom cover art entirely.** Root cause: `CustomCoverPicker` (`LibraryView.swift`) had `showCamera` (or `showPhotoPicker`) and `showReview` as two independent boolean-driven `.fullScreenCover`s on the same view — the capture-complete handler set `showReview = true` in the same instant the camera/picker cover was dismissing itself, so SwiftUI was asked to animate two full-screen transitions on one view at once and got stuck. Fixed by sequencing the transition properly: capture now only stores `pendingReviewImage`, and a new `presentReviewIfNeeded()` helper (called from `showCamera`'s `.fullScreenCover(..., onDismiss:)`, from `showPhotoPicker`'s dismissal via `.onChange`, and after the photo-library `Task` finishes loading data) only flips `showReview = true` once the prior cover has actually finished closing and an image is ready. Also clears `pendingReviewImage` before opening the camera/picker (so a cancelled capture can't leak a stale image into review) and after the review screen confirms/cancels. Full project builds clean, 0 errors. **Not yet re-tested on device.**
  - **Follow-up 2026-08-08 — Take Photo still froze after the fix above (unconfirmed whether retested on a fresh rebuild), plus a second silent-failure bug found via a workaround attempt.** Adele tried a workaround: cut a "sticker" out of a photo using Photos' own lift-subject feature, saved it to her camera roll, then picked it via Choose from Library — the app just sat on the Edit Book screen with no change and no error, i.e. it silently did nothing. Root cause: `CustomCoverPicker`'s `.onChange(of: selectedItem)` used `try? await newItem?.loadTransferable(type: Data.self)` — if that throws or returns data that isn't a valid `UIImage` (plausible for a Photos-exported sticker PNG), the `try?` swallows it and nothing happens, no feedback to the user. Fixed by adding a `loadError: String?` shown under the "Add Cover" menu and a proper `do/catch` around the load that surfaces "Couldn't load that photo — try picking a different one." instead of failing silently — same principle already applied earlier to the ISBN-not-found flow (`BookMetadataProvider.swift`). Full project builds clean, 0 errors. **Whether this was the actual cause of the "stuck" screenshot Adele sent is unconfirmed — needs a real-device retest.**
  - **Confirmed 2026-08-08 — Take Photo freeze persisted even after a genuine rebuild, so it wasn't just a stale-build issue; root-caused and fixed differently.** Adele explicitly confirmed her retest was on a fresh rebuild and it still froze, ruling out the earlier onDismiss-timing fix as sufficient. Real root cause: Take Photo and the background-removal review screen were two *separate* `.fullScreenCover`s handing off to each other (dismiss camera cover → present review cover) — on real hardware, tearing down the camera's live capture session apparently doesn't complete cleanly in time for SwiftUI to reliably start the next cover's presentation, regardless of using `onDismiss` to sequence it. Fixed by removing the handoff entirely: added `CameraCaptureFlowScreen` (`CameraCoverCapture.swift`) which holds both the camera capture UI and the review UI as internal `@State` inside **one** continuous `.fullScreenCover` — switching from "capturing" to "reviewing" is now a plain SwiftUI content swap with no modal transition to race. `RawPhotoCaptureScreen` no longer self-dismisses via `@Environment(\.dismiss)`; it takes an explicit `onCancel` closure like everything else, and `CustomCoverPicker` (`LibraryView.swift`) now presents `CameraCaptureFlowScreen` directly behind a single `showCamera` boolean. The photo-library path (`showPhotoPicker` → `showReview`) still uses two covers since `.photosPicker` is a separate system presentation that can't be merged the same way, but keeps the earlier onDismiss-based sequencing fix, which hasn't itself been reported broken. Full project builds clean, 0 errors. **Not yet re-tested on device.**
  - **Fixed 2026-08-08 — "Kate's red journal" card visibly taller than its neighbors in the Library grid, confirmed via screenshot (its "Add" button sat noticeably lower than the books beside it).** `BookTileView`'s cover box (`LibraryView.swift`) computed its size via `.aspectRatio(120.0/170.0, contentMode: .fit)` applied directly to the `ZStack` **containing** the book's `Image` — in SwiftUI, sizing a container via `aspectRatio` while an actual (especially large, camera-resolution) image sits inside it can let that image's own layout negotiation influence the computed frame size, so different photos could yield very slightly different tile heights despite identical modifiers. Fixed by moving the aspect-ratio sizing onto a plain `Color.clear` first (which has no content-driven sizing quirks and reliably obeys `aspectRatio`+`frame` alone), then drawing the actual cover image on top via `.overlay` — the image now purely fills whatever box `Color.clear` already decided, and can never feed back into that decision. Full project builds clean, 0 errors. **Not yet re-tested on device.**
- [x] In-app ISBN search — already implemented via OpenLibrary, confirmed working live (tested with a real ISBN, returned correct title/author/cover).
- [x] **Crash found live via Xcode debugger 2026-08-08: deleting a book from the Library that was currently placed on the Table crashed with `Fatal error: This model instance was invalidated` (`Book.coverImageData.getter`, called from `TableBookView.coverContent`). Fixed, not yet re-tested on device.** Root cause: `ComposedBook.book` (`Models/Book.swift`) is a relationship with no inverse link back to `Book`, so SwiftData has no way to automatically clear a table placement when its book is deleted — `LibraryViewModel.deleteBook` just called `modelContext.delete(book)`, leaving any `ComposedBook` still on the table pointing at the now-deleted object. The next time the Table read that book's cover, the app hard-crashed. Fixed by adding `removeBookFromCompositions(_:)` to `LibraryViewModel.swift`, called before the delete — it fetches every `CoffeeTableComposition` and strips out any `ComposedBook` referencing the book being deleted, same as the existing "remove from table" flow does. Full project builds clean, 1 pre-existing unrelated warning (`TabloView.swift:97`, `UIScreen.main` deprecation). **Needs a real-device retest**: add a book, place it on the Table, delete it from the Library, confirm the Table no longer shows/crashes on it.
- [ ] Title search (type a book title instead of ISBN and get matching results to pick from) — explicitly deferred by user 2026-08-07 ("we'll leave that for now"), wants it added at a later date. Not yet built — distinct from ISBN lookup, needs a results-picker UI since a title can match multiple editions.
- [x] **Snapshot & export** — `Views/TableSnapshotView.swift` (clean, chrome-free render of the composition via `ImageRenderer`) + `ShareSheet` (UIActivityViewController wrapper), wired to a new "Share" button in `TabloView`'s control bar. Verified working in Simulator on iPhone — Share button renders the composition and presents the system share sheet without crashing.
- [x] Watermark on exported/shared images — restyled 2026-08-08 per Adele's choice: moved from a faint bottom-right "Tablo" text to a bottom-center "Made on Tablo" stamp on a semi-transparent dark pill (`TableSnapshotView.swift`), so it stays legible on light and dark surfaces alike (the old version could wash out on light surfaces). Also added a pre-filled share caption ("Here's what's on my Tablo right now 📚✨") via `ShareLink`'s `message:` parameter (`TabloView.swift`). Paid option to remove the watermark (monetization angle) still just an idea, not scoped. **Not yet visually confirmed** — build succeeds, needs a real share test.
- [x] **Bug: Share sheet renders blank, then crashes — fixed and confirmed working 2026-08-08 on Adele's real device.** First tried two targeted patches to the custom `ShareSheet`/`UIActivityViewController` wrapper (deferring the `ImageRenderer` read; fixing a self-referencing popover source view) — both built fine but Adele confirmed on her real iPhone the sheet was still exactly as broken. Replaced it entirely with SwiftUI's native `ShareLink` (`TabloView.swift`), which handles all `UIActivityViewController` presentation internally instead of us wiring it by hand. **Confirmed by Adele:** the share sheet now populates correctly (AirDrop, Messages contacts, Copy, Print, etc. all appear).
- [x] **Bug found during that same test: shared image was missing decorations/stickers and looked stale. Confirmed fixed 2026-08-08 on real device.** `TabloView`'s cached `shareImage` (rendered via `ImageRenderer` for the `ShareLink`) was only refreshed on first appear and when tapping to reveal the control bar — not when returning from the Style view, where books/decorations actually get added or moved. So sharing right after editing could show an old snapshot missing recent changes. Fixed by also refreshing on dismiss of both the Library and Style full-screen covers (`TabloView.swift`'s `.fullScreenCover(..., onDismiss: refreshShareImage)`).
- [x] **Regression fix: dragging books became jittery/slow after the delete-button restructuring above — confirmed fixed by Adele** ("it moves beautifully now"). Root cause and fix as previously described (gesture attachment point moved back to after the scale/rotate/position transforms).
- [x] **Root-caused and fixed: sticker/decoration artwork couldn't be resized (books were fine). Confirmed working 2026-08-08 on real device.** The 88×88pt touch-target enlargement alone didn't fix it — Adele confirmed still broken on retest, narrowing it specifically to *resize* (pinch), not drag. Real root cause: `DecorationView.swift` had `.gesture(dragMagnifyRotateGesture)` and a separate, uncomposed `.onTapGesture { onSelect() }` stacked on the *same* view — two independent gesture attachments on one view is a known source of exactly this flakiness (simple drags mostly work, multi-finger pinch/rotate gets starved). `TableBookView` never had this problem because its tap-select and its drag/pinch/rotate gestures live on different views. Restructured `DecorationView` to match: split into `coverContent` (carries the tap-select `.highPriorityGesture`) and a sibling `deleteButton`, with `.gesture(dragMagnifyRotateGesture)` on the outer transformed view — the same pattern already proven to work for books.
- [x] **Bug found during Share re-test: Share button appeared visibly greyed out and tapping it did nothing.** Root cause: during the `ShareLink` rewrite, the `DispatchQueue.main.async` deferral around reading `renderer.uiImage` got dropped — `refreshShareImage()` was reading the rendered image synchronously again, immediately after creating the `ImageRenderer`, which is the exact same premature-read bug from the original blank-share-sheet issue. Re-added the deferral. **Confirmed the button styling itself now looks normal after this fix**, but see the crash below found in the same test round.
- [x] **Real crash found via Xcode debugger while sharing to Messages: `Thread 1: signal SIGKILL`, Energy Impact: High. Confirmed fixed 2026-08-08 — re-tested share-to-Messages on real device, no crash.** Adele was running the app from Xcode on her real device ("Adele's iPhone") when the OS force-killed it mid-share — a resource/watchdog termination, not a code-level crash (no app-code stack trace, just a kill signal). Root cause: passing a `SwiftUI.Image` directly as the `ShareLink` item is not an efficient path for destinations like Messages, which need real file bytes to attach — likely caused excessive CPU/energy use converting/serializing the in-memory `Image` representation. Fixed by sharing actual PNG `Data` instead: added a small `ShareableTableSnapshot: Transferable` (`TabloView.swift`) wrapping `Data` via `DataRepresentation(exportedContentType: .png)`, with `sharePNGData` computed alongside `shareImage` in `refreshShareImage()`. This is the standard, lightweight way apps share images via `ShareLink`.
- [x] **Bug: black line at bottom of shared snapshot — fixed and confirmed 2026-08-08.** The surface-image `scaleEffect` anchor had drifted from `.bottom` to `.center` (likely during an earlier edit), letting the scaled edge fall short of the frame's bottom edge and show the render background through as a black line. Reverted to `.bottom`, the confirmed-working setting (`TabloView.swift`, commit `5c5c2cb`). Also added a suggested filename (`MyTablo.png`) to the shared PNG item.
- [x] **"Tokyo & Beyond" duplicate-book question — resolved 2026-08-08, not a code bug.** Adele confirmed this was caused by her phone acting up during testing, not an app issue. No code change needed.
- **Session paused 2026-08-08, end of a long real-device test round — handoff notes for next session:**
  - Everything above through commit `4f458ff` is committed and pushed to `widget-feature`. Working tree is clean, nothing uncommitted.
  - Late in this session, Adele reported "none of Library, Style, or Share respond, can't edit at all" on her real device. Investigated live: Xcode showed "Launching 'ReadingTable' is taking longer than expected... LLDB is likely reading from device memory to resolve symbols" — a known Xcode/LLDB slow-attach dialog, most likely caused by this session's many back-to-back rebuild/reinstall cycles (multiple bundle ID changes, repeated Run cycles), not a code bug. **This was not root-caused as an app bug and was not code-fixed — likely a false alarm, but not confirmed.** A separate SIGKILL screen shown around the same time had Energy Impact: **Zero** (contrast with the earlier *real* crash's Energy Impact: **High**), consistent with it being an artifact of the debug session stopping/restarting rather than a live resource crash.
  - **Resolved 2026-08-08:** confirmed false alarm. After a clean, fully-settled launch, Library/Style/Share all respond to taps normally — no code fix was needed.
  - All previously-outstanding re-tests now confirmed working on real device 2026-08-08: shared image including recent decorations, sticker resize/rotate, Share-to-Messages no longer crashing, delete-book-from-table, and the black-line-in-shared-snapshot fix. The duplicate-book question (two "Tokyo & Beyond" cards) is still open — not yet confirmed with Adele whether that was a real bug or two intentional placements.
  - Adele intends to start the next session from her iPhone (likely the Claude mobile app, not Claude Code) so she can send live photos while testing. **Important limitation to flag to her if not already clear:** a mobile Claude session has no access to this Mac, this repo, Xcode, or git — it can look at photos and reason about likely causes, but can't read/edit the actual Swift files, run builds, or commit/push. Any code fix identified there still needs to be carried back to a Claude Code session on this Mac to actually implement, verify by building, and push.
- [x] **Grid-snap + double-tap-to-straighten** for books on the table — "Snap to Grid" toggle button (`square.grid.2x2` icon) in Style view rounds drag position to a 40pt grid (`CoffeeTableViewIntegrated.swift`); double-tapping a book resets its rotation to 0 without moving it. Both verified working live in Simulator.
- [ ] Quick-swap surface themes: gesture or menu to cycle table textures.
- [x] **Sticker packs** — added "Coffee House" (21) and "Fall Desk" (16) as new tabs in `DecorationView.swift`'s `StickerPickerView`, alongside the existing "Desk Plants" tab (tabs-by-pack UI, `StickerPack` enum). Verified all three tabs render correctly in Simulator. Remaining packs not yet requested: Desk Supplies, Pastels Washi Tape, Witchy Desk Supplies, Ultimate Crystal Collection (94 stickers), plus the 33-card "Fall Oracle Deck" (different vibe, tarot-style — flagged as maybe not fitting the desk-decor theme). Source files in `~/Documents/Curio Design Assets/Stickers/`.
- [ ] More Kate table textures: `~/Documents/Curio Design Assets/Table Surfaces/Kate TableSkapes/` has 7 unimported "-2" variant photos (antiquewood-2, concrete-2, linen-2, marble-2, steel-2 [new material], whiteplaster-2, wood-2) not yet added to the surface picker. Lower priority than stickers per user.
- [ ] Future idea (2026-08-07): a "radio" decoration/object on the table that plays the user's music through their phone (e.g. Apple Music) when tapped — a fun future exploration, not scoped yet.
- [~] **Home screen widget — "Current Read" (started 2026-08-08, in progress).** New `ReadingTableWidgets` extension target added, showing a small (2×2) widget with the most-recently-added book's cover, title, and author (`ReadingTable/Views/Widgets/SmallWidgetView.swift`). No real "currently reading" flag on `Book` yet, so it's standing in for that — **this is expected placeholder behavior, not a bug.** No configuration UI on the widget itself either (can't pick a different book from the widget — by design so far, nothing to fix). Required sharing the SwiftData store with the extension via an App Group (`group.com.adeleroberts.ReadingTable`, entitlements on both targets) — `ReadingTableApp.swift` now opens the model container at a shared store URL and one-time-migrates any pre-existing local database into it so old books aren't lost. `LibraryViewModel` calls `WidgetCenter.shared.reloadAllTimelines()` on add/edit/delete so the widget refreshes. Removed Xcode's unused template boilerplate (`AppIntent.swift`, the "Timer" Control Widget, the "Hello 😀" Live Activity) so only the real widget ships.
  - **Verified 2026-08-08 (Claude, Simulator):** full project (both targets) builds clean with 0 errors, 2 pre-existing unrelated warnings (`TabloView.swift:86`, iOS 26 `UIScreen.main` deprecation). App launches fine against the new shared store with existing books intact.
  - **Verified 2026-08-08 (Adele, real device):** widget successfully added to a real home screen and renders — confirms the App Group / shared-store / widget-extension plumbing all actually works end to end, not just in Simulator.
  - **Follow-up 2026-08-08 — same crash still hit after rebuilding, root-caused to stale on-device data, repaired.** After rebuilding with the `removeBookFromCompositions` fix above, Adele hit the identical `Book.coverImageData.getter` / "This model instance was invalidated" crash again just from reopening the app — no new delete action. Root cause: that fix only prevents *future* corruption; it doesn't repair a `ComposedBook` row that was already saved to her phone's on-disk store pointing at a book deleted during earlier testing, before the fix existed (`ComposedBook.book` in `Models/Book.swift` has no inverse relationship, so the dangling pointer persists across rebuilds/relaunches — Stop+Run reinstalls the binary but doesn't touch the SwiftData store). Fixed by adding `repairDanglingBookReferences()` to `CoffeeTableCompositionViewModel.loadCompositions()` — runs on every load, compares each `ComposedBook.book`'s `persistentModelID` (safe identity check, doesn't touch the possibly-invalidated object's stored properties like `coverImageData`) against the set of currently-valid `Book` IDs, and strips out any composition item pointing at a book that no longer exists. Full project builds clean (`xcodebuild`, iphonesimulator, 0 errors). **Not yet re-tested on device** — next step is Adele rebuilding and relaunching once more; this time the repair should clean up the stale reference automatically on launch, no manual data reset needed.

**Fixed 2026-08-08 — crooked cover art in widget.** Root cause: `CustomCoverPicker` in `LibraryView.swift` saved photos picked via `PhotosPicker` as raw `Data`, keeping only the EXIF orientation tag rather than upright pixels. The main app's UIKit-based rendering honors that tag, but WidgetKit's rendering doesn't reliably — so a normal portrait photo (sensor data sideways + a rotation tag, which is how cameras store portrait shots) looked straight in-app but rotated in the widget. Fix: added a `private extension UIImage { normalizedOrientation() }` (`LibraryView.swift`) that bakes the orientation into the pixel data via `UIGraphicsImageRenderer` before saving, so it's correct everywhere regardless of who renders it. Committed as `7f58539` on the `widget-feature` branch. **Not yet visually confirmed** — built a synthetic test JPEG reproducing the exact bug condition (900×600 sideways pixels tagged with EXIF orientation 8, confirmed via `CGImageSourceCopyPropertiesAtIndex`) and loaded it into the Simulator's photo library, but couldn't drive the picker UI to select it — see tooling note below.
  - **New bug found 2026-08-08, reported by Adele testing the real widget (not yet investigated) — watermarked/text-covered artwork showing in the widget.** The widget picked up a book whose stored cover art has visible white text baked into the image itself. Adele's read, which sounds right: this isn't a widget rendering bug like the orientation one above — it looks like the *stored* `coverImageData` for that book already contains the text (most likely an exported/shared Tablo snapshot, which has the "Tablo" watermark from `TableSnapshotView.swift` baked in, that got saved back in as a custom cover via `CustomCoverPicker` at some point). Since there's no way to edit which book/cover the widget shows from the widget itself, the fix has to happen on the book's cover data in the Library. **Next steps once back online:** (1) find the affected book in the Library grid and check/replace its cover via `EditBookView`, (2) confirm whether this was a one-off bad test photo or something the app could actually let happen (e.g. no guard against picking an already-exported Tablo image as a cover) — if the latter, decide whether it's worth guarding against.
  - **Tooling note 2026-08-08 — synthetic Simulator taps not registering.** Across two devices (iPhone 17, iPhone 17 Pro), coordinate-based taps stopped producing any effect on-screen — confirmed even at the home-screen level (tapping the Tablo icon did nothing), while the HOME hardware-button action still worked. This blocked visually confirming both the orientation fix and adding a widget to a Simulator home screen this session. Treated as an environment/tooling issue, not an app bug — matches a similar note logged 2026-08-08 earlier about the widget-gallery jiggle-mode menu. Re-test once tooling is behaving normally.
  - **Tooling note 2026-08-08 (recurrence, later session) — same tap issue, narrowed further.** Recurred on iPhone 17 during release-readiness fixes. This time narrowed down: a plain `.onTapGesture` on a `Color.clear` layer (e.g. the "tap to reveal control bar" gesture) DID respond to synthetic taps, but native SwiftUI `Button` controls (Library/Style/Share, and a `touch_path` press-and-hold variant) did NOT respond, even immediately after the working gesture fired. Confirmed this is not a regression from the same-session code changes — it blocked verifying multiple unrelated fixes. Treat as an environment/tooling limitation specific to `Button`-type controls in this automation setup, not an app bug, until proven otherwise on a real device or a fresh Simulator session.
  - **Git status 2026-08-08:** both widget commits (`77475e3` scaffold, `7f58539` orientation fix) are on the local `widget-feature` branch. `git push`/`git fetch` from Claude's tool session fail with `could not read Username for 'https://github.com'` — no GitHub credentials available in that sandboxed environment, separate from Adele's own network issues. Adele pushed successfully herself via GitHub Desktop (same working copy) while working off a phone tether (no building Wi-Fi) — confirmed by the local repo's own remote-tracking ref, not independently re-verified against GitHub from Claude's side. If a future session finds `widget-feature` local/remote out of sync, trust GitHub over any stale local assumption.
  - **Update, 2026-08-08 (later session): this credential limitation no longer holds.** `git push origin widget-feature` succeeded directly from Claude's tool session (commit `039a37e`, the record-player Music rebuild) with no separate auth step — the sandbox now has working GitHub credentials. Treat "Claude can't push" as no longer a safe assumption; try `git push` directly first, and only fall back to asking Adele to push via GitHub Desktop if it actually fails.
  - Full Tablescape/Library Shelf widget vision (below) is still not started — this is just the first small step.
  - **Requested 2026-08-08, not yet scoped — favorite/"Currently Reading" flag on `Book`.** Adele asked whether books can be favorited or marked as a highlight/"currently reading," specifically so the widget shows something the user chose rather than just the most-recently-added book (the current placeholder behavior noted above). Would need a new field on the `Book` model (e.g. `isCurrentlyReading: Bool` or a favorites concept), a way to toggle it from the Library UI, and `ReadingTableWidgets.swift`'s `TimelineProvider` updated to prefer that book over "most recent." Remember any `@Model` field addition must default rather than be required, so existing local records stay loadable.
  - **Future idea (2026-08-08) — auto-fill the Manual add form via text recognition (OCR).** Adele wants the app to read the title/author straight off a photographed cover to populate the new Manual-entry form (added this session) automatically, rather than typing them by hand. Feasible with Apple's Vision framework (`VNRecognizeTextRequest`) run against the same image captured by Scan Cover/Take Photo — not scoped or started.
- [x] **Bug (regression?): book z-order stuck — root-caused, fixed, and confirmed working 2026-08-08 on Adele's real device.** Root cause: `.onTapGesture { selectedBook = nil; selectedDecoration = nil }` was attached directly to a `Group` in `CoffeeTableViewIntegrated.swift` that wrapped the books, decorations, *and* the control-bar buttons. `Group` is "transparent" for modifiers — SwiftUI propagates a modifier applied to a `Group` onto every child individually, rather than treating the group as one view. So every tap on a book (meant to select it and call `bringToFront`) also fired a second, competing gesture on that same view that immediately cleared the selection back to `nil`. Fix: moved the deselect gesture to its own dedicated background layer instead of the `Group`. The same class of bug existed in `TabloView.swift`'s "tap to reveal control bar" gesture — fixed the same way. **Confirmed by Adele on her real iPhone:** single-tapping a partially-hidden book now shows the delete X and visually brings it to the top layer, reliably.
- [x] **Bug: can't delete a book from the table — separate root cause found and fixed 2026-08-08, not yet re-tested.** This was a distinct bug from the z-order one above (the delete X was visible — selection worked — but tapping it did nothing). Root cause: `TableBookView`'s `.highPriorityGesture` (used to tell single-tap-select apart from double-tap-straighten) was attached to the *entire* card, and `.highPriorityGesture` explicitly takes priority over gestures defined by views inside it — including the delete `Button` nested on top of it. So tapping the delete X was still being interpreted as "select the card" underneath it. Fixed by restructuring `TableBookView` so the delete button is a sibling overlay outside the gesture-bearing view, not a descendant of it (`coverContent` now carries the gestures; `deleteButton` sits alongside it in the outer `ZStack`). Checked `DecorationView.swift` (sticker delete) for the same pattern — it uses a plain `.onTapGesture`, not `.highPriorityGesture`, so it wasn't affected and wasn't reported broken. Also replaced every `try? modelContext.save()` in `CoffeeTableCompositionViewModel.swift` with a `save()` helper that surfaces real errors via a new alert in `StyleView`, as defense-in-depth. **Confirmed fixed 2026-08-08 on real device** — the delete X now actually removes the book.
- [x] Exploration: iPad support and testing. **Confirmed working in Simulator 2026-08-07** — built and launched on iPad Pro 13-inch (M5) simulator. Table view, tap-to-reveal control bar, Library grid, Add Book sheet, and the Stickers picker all render correctly at iPad size (sheets stay centered/sized rather than stretching full-width). No iPad-specific layout bugs found. Noted in passing: the "Library" label in `TabloView.swift`'s control bar truncates to "Libr…" on both iPhone and iPad — pre-existing cosmetic bug, not iPad-related, not yet fixed.
- [ ] Exploration: "nightstand" mode.
- [ ] Exploration: native clock display on the table.

## Release-readiness pass for App Store launch (logged 2026-08-08)
Working toward a back-to-school App Store launch. Full P0/P1/P2 audit done; P0 code fixes below are complete for this session (all build clean, 1 pre-existing unrelated warning). None of tonight's fixes have been visually re-confirmed on-screen yet — see tooling note above (synthetic Simulator taps on `Button` controls not registering this session).

- [x] **Bundle identifier fixed.** Was malformed/tripled (`com.adeleroberts-com.adeleroberts-com.adeleroberts.ReadingTable`) — confirmed with Adele it was not yet registered anywhere, so changed to `com.adeleroberts.Tablo` (main app) and `com.adeleroberts.Tablo.Widgets` (widget extension) in `project.pbxproj`. Repo name, Xcode project name, and target names are unchanged.
- [x] **App Group migration data-loss risk fixed.** `ReadingTableApp.swift`'s one-time migration to the shared widget store previously marked itself "done" (via `defer`) before checking whether the file copy actually succeeded — a failed copy would silently lose a user's existing library on update and never retry. Now only marks migration done when there was nothing to migrate (fresh install) or the copy genuinely succeeded; a failed copy will retry on next launch.
- [x] **Silent "Unknown Book" placeholder removed.** `BookMetadataProvider.swift` previously saved a fake "Unknown Book / Unknown Author" entry with no warning when an ISBN wasn't recognized. Now throws a proper `BookMetadataError.notFound`/`.lookupFailed`, surfaced to the user via the existing error-message UI in `LibraryViewModel`.
  - **Bug found on real-device test 2026-08-08 — custom covers looked cropped/oversized in the Library grid, though fine in the Add/Edit preview.** Root cause: the Add/Edit cover preview (`CustomCoverPicker`) uses a 120×170 frame (aspect ratio 0.71), but `BookTileView`'s Library grid tile (`LibraryView.swift`) used a fixed 140pt height with a flexible width — a noticeably wider/shorter shape. Since covers render with `.scaledToFill()`, the same image got cropped far more aggressively in the grid than in the preview the user approved, cutting off titles near the top — most visible on covers with little margin, e.g. Scan Cover output, which crops tight to the book already. Fixed by giving `BookTileView`'s cover the same 120:170 aspect ratio as the preview (`.aspectRatio(120.0/170.0, contentMode: .fit)` instead of a fixed height), so cropping now matches what's shown when the cover is picked. Left `TableBookView` (Style/Table view) alone — its existing 112×160 frame is already aspect 0.70, effectively identical to the preview, so it wasn't the source of the mismatch and changing it risked shifting already-saved book positions on the table (position/scale are stored in absolute coordinates tied to that frame size). Full project builds clean, 0 warnings. **Needs a real-device retest** — confirm "Outsmarting Reality," "Human Design Unlocked," and Kate's red "Notes." journal all display full covers (no cropped title) in the Library grid now.
  - **Bug found same session — the ISBN field kept the previous book's value when reopening Add Book.** Most noticeable after using Manual mode to add a book whose ISBN lookup had failed — `addBookManually` never cleared `viewModel.isbnInput`, so the failed ISBN sat there for the next add. Fixed by clearing `viewModel.isbnInput` whenever the Add Book sheet is dismissed for any reason (`LibraryView.swift`'s `.sheet(..., onDismiss:)`), so it always starts blank next time regardless of how the previous add finished.
  - **Follow-up 2026-08-08 — manual entry added, closing the gap left by that fix.** Adele hit a real ISBN-not-found case live (`9781068486005`, "Outsmarting Reality") and asked for a way to add a book by hand when lookup fails, plus a way to add items with no ISBN at all (journals, notebooks). Added `addBookManually(title:author:coverImageData:)` to `LibraryViewModel.swift` (generates a synthetic `"manual-<UUID>"` value for the unique `isbn` field, since that column isn't optional — the field is otherwise unused elsewhere in the codebase, so this is safe). `AddBookView` (`LibraryView.swift`) now has a segmented "By ISBN" / "Manual" picker: Manual mode swaps the ISBN field for Title + optional Author fields, keeps the same cover picker, and skips the OpenLibrary lookup entirely. When an ISBN lookup fails, the error message now also shows an "Enter details manually instead" button that switches modes directly. Full project builds clean, 0 warnings (2 pre-existing unrelated `UIScreen.main` deprecation warnings only). **Not yet tested on device.**
- Outstanding from the audit, not yet started — handed to Adele's ChatGPT ("GG") for drafting since it's writing/research rather than code: Apple privacy manifest (`PrivacyInfo.xcprivacy`), Privacy Policy + Support page copy, and a specific list of accessibility-label/tap-target fixes (8 unlabeled icon-only buttons, 4 undersized tap targets) for Claude to apply once drafted.
- Also flagged, not yet decided: whether to keep iPad in the v1.0 `TARGETED_DEVICE_FAMILY` given limited iPad-specific testing time before launch (the share-sheet bug itself affects iPhone too, so it isn't iPad-specific).

### Future ideas — "app within an app" desk widgets (logged 2026-08-07, explicitly deferred, not scoped)
User brain-dumped a big batch of future decoration/widget ideas, explicitly asked to save for later rather than build now:
- Post-it notes decoration.
- Calendar widget pulling from the iPhone's actual calendar data (would need EventKit + permission).
- A little map/globe showing current location (would need Core Location + permission).
- Weather display widget (would need WeatherKit).
- Music player linked to Apple Music, playable from the table (ties into the existing "radio" idea above — same feature, more detail: wants real playback control, not just a decorative radio).
- Miniature versions of stock iPhone apps as table decorations: alarm, world clock, timer, stopwatch, etc. — a genuine "mini apps on the table" concept.
- Instant-camera decorations (Polaroid/Fuji/Kodak style) — both as decorative artwork (mini camera objects) and as functional print-style photo frames.
- Let a user add their own photo and have it appear inside a blank instant-photo-style frame — Kate reportedly already has blank Polaroid-style artwork ready for this.
- User framed several of these as "maybe future" themselves — treat this whole section as low-priority exploration, not a commitment, until re-raised.

**Re-raised 2026-08-08 with real priority signal — this is no longer just Adele brainstorming.** Adele asked Kate (an actual end user of the app, not just its designer) what she'd want next, live, and Kate's wishlist maps almost exactly onto this list: a flip-calendar pulling in real phone calendar events, clocks/alarms/world clock, sticky notes, a media player for music, weather, and a blank open-journal-page decoration (new — not previously logged; sounds like a writable/blank journal-style decoration, needs clarifying what "open journal page" means before scoping). Adele asked for a recommendation on what to tackle first rather than build immediately — **Claude's assessment logged here for next session:**
- **Easiest, do first:** Sticky notes (reuses the existing `DecorationView.swift` sticker/decoration system — just needs an editable-text variant, no new permissions or Apple entitlements).
- **Next, most-requested:** Calendar (EventKit, needs a permission prompt but no special Apple Developer entitlement — read-only display of real events on a flip-calendar-style decoration).
- **Then, moderate:** A simple clock/world-clock decoration showing live time — but note this can only be a *decorative* clock, not real integration with the iPhone's actual Alarms app, since Apple doesn't expose a public API for that.
- **Higher effort, needs Apple Developer entitlement setup:** Weather (WeatherKit) and location (Core Location) — both doable but require enabling capabilities in the Apple Developer account first, not just code.
- **Most complex, save for last:** Real Apple Music playback control (MusicKit) — significant UI (search, now-playing, picker) plus a subscription/entitlement dependency.

**Order confirmed by Adele 2026-08-08: Sticky Notes first, then Calendar next session.**

- [x] **Sticky Notes — built 2026-08-08, not yet tested on device.** Reused the existing `Decoration` model/gesture system rather than adding a new one: `Decoration` (`Models/Decoration.swift`) gained optional `noteText`/`noteColorName` fields (default nil, so existing saved stickers stay loadable) and an `isStickyNote` flag keyed off a sentinel `imageName`. `DecorationView.swift` branches its rendering — a plain image for existing stickers, or a colored rounded-rect "paper" with the note text overlaid, in one of 4 pastel colors (`StickyNoteColor`) — so notes automatically drag/resize/rotate/delete/z-order and show up in Table view and shared snapshots exactly like any other decoration, no extra plumbing needed there. Adding/editing text uses a new `StickyNoteEditorView` sheet (title + color swatches): a new "note.text" button in Style view's control bar creates one, and a pencil-icon button (next to the existing delete X, shown only on sticky notes when selected) reopens the same sheet pre-filled to edit it. `CoffeeTableCompositionViewModel` got matching `addStickyNote`/`updateStickyNoteText` methods alongside the existing decoration ones. Full project builds clean, 1 pre-existing unrelated warning (`TabloView.swift`, `UIScreen.main` deprecation). **Confirmed working 2026-08-08 on Adele's real device** — added a note, text/color/rotation all render correctly on the table and carry through into a shared snapshot.
  - **Fixed 2026-08-08 — pencil "Edit" button on an existing sticky note opened a blank "New Sticky Note" sheet instead of pre-filled Edit mode.** Applied the planned fix exactly as scoped: replaced `showStickyNoteEditor`/`editingStickyNote` with a single `StickyNoteEditorContext: Identifiable` struct and `@State private var stickyNoteEditorContext: StickyNoteEditorContext?`, driving the sheet via `.sheet(item:)` instead of `.sheet(isPresented:)` (`CoffeeTableViewIntegrated.swift`). Full project builds clean, 1 pre-existing unrelated warning. **Not yet re-tested on device** — Simulator's synthetic taps can't click through `Button` controls this session (same tooling limitation logged elsewhere in this file); needs a real-device retest (edit an existing note, confirm it updates in place).
  - **Verified 2026-08-08 — double-tap-to-straighten on decorations confirmed correctly wired**, no changes needed; builds clean alongside the fix above. Same Simulator tooling limitation blocks visual re-confirmation this session — needs a real-device retest.
  - **Added 2026-08-08 — more visible shadow under sticky notes, confirmed working on real device.** Per Adele's request (they read as flat pastel squares with no lift off the table). `DecorationView.swift`'s sticky-note branch now gets its own `.compositingGroup()` + stronger `.shadow(...)` (opacity 0.28–0.35, radius 6–9) instead of sharing the same subtle shadow used for stickers/photo frames (opacity 0.18–0.3, radius 4–8). **Confirmed 2026-08-08** — Adele shared a real-device screenshot showing three notes ("I love Kate", "Hi Mum", "Claude is the best coder EVER!!!") all with a clear, tangible shadow lifting them off the linen surface.

- [x] **Calendar decoration — built 2026-08-08, builds clean, not yet tested on device.** Next item in the confirmed Sticky Notes → Calendar order. Live, read-only display of today's real events on a flip-calendar-style card, using EventKit — no new stored fields needed (unlike sticky notes/photos, the data is fetched live each time, nothing is saved to the `Decoration`). Added `Decoration.calendarImageName` sentinel + `isCalendar` flag (`Models/Decoration.swift`), mirroring the sticky-note/photo-frame pattern. New `Services/CalendarService.swift` (`@MainActor`, `ObservableObject`) wraps `EKEventStore`, requesting full calendar access via `requestFullAccessToEvents()` and fetching just today's events, sorted and capped at 3. `DecorationView.swift` renders it via a new `CalendarDecorationContent` subview — red weekday strip, big day number, month, then up to 3 event titles or a "No events today" / "Enable Calendar access in Settings" placeholder — fetched in `.task` when the card first appears, so it drags/resizes/rotates/deletes/z-orders like every other decoration with no extra plumbing. Deliberately has no in-decoration "Allow Access" button — past bugs in this file showed a tappable control nested inside a gesture-bearing view gets starved by the outer drag/tap gestures, so it relies on the automatic system permission prompt instead. A calendar-icon button in Style view's control bar (`CoffeeTableViewIntegrated.swift`) adds one via the existing generic `compositionViewModel.addDecoration(imageName:)` — no new view-model method needed. Added `INFOPLIST_KEY_NSCalendarsFullAccessUsageDescription` to both Debug/Release configs of the main app target in `project.pbxproj` (iOS 17+ key; deployment target is 26.5 so the legacy pre-17 key isn't needed).
  - **Privacy caption extended to match, per Adele's explicit instruction 2026-08-08.** Real event titles are now live personal data that can appear in a shared snapshot, same category as photos. `TabloView.swift`'s `hasPhotoDecorations` boolean was replaced with a `sharingPrivacyNotice` computed property that checks photos and calendar independently and only shows a caption when at least one is actually present — sticky notes and ordinary stickers still don't trigger it, exactly as instructed (Adele was explicit: "Sticky notes alone should not trigger it"). Wording adapts to mention photos, calendar events, or both depending what's on the table.
  - **Build note:** first build failed — `CalendarService.swift` didn't compile (`Type 'CalendarService' does not conform to protocol 'ObservableObject'`, `Initializer 'init(wrappedValue:)' is not available due to missing import of defining module 'Combine'`), caught live by Adele from her own Xcode. This project needs an explicit `import Combine` in any file using `@Published`/`ObservableObject` (matches the existing architecture note that view models still import Combine even though the app doesn't rely on it otherwise). Added the import; rebuilt clean.
  - **Confirmed working 2026-08-08 on Adele's real device** — permission prompt appeared, calendar decoration rendered correctly on the table and in a shared snapshot, and the privacy notice correctly disappeared once photos and the calendar were both removed from the table.
  - **Answered 2026-08-08 — which calendar does it read from?** `CalendarService.fetchTodaysEvents()` calls `store.predicateForEvents(withStart:end:calendars: nil)` — passing `nil` for calendars means it queries every calendar the device has access to (iCloud, Google, Outlook, whatever's configured), not just one default. No code change needed, just confirmed for Adele.
  - **Bug found and fixed 2026-08-08 — main Table view didn't pick up a newly-added event, though Style view did.** Adele added an all-day event via the Calendar app, returned to MyTablo, and the calendar decoration still said "No events today" on the main Table view — but opening Style view showed it correctly. Root cause: `CalendarDecorationContent`'s `.task` (`DecorationView.swift`) only runs once, the first time that view instance is created. The Table view's decoration view stays alive across backgrounding/foregrounding, so it never re-ran; Style view is a fresh `fullScreenCover` each time it's opened, so it always got a brand-new instance that re-fetched. Fixed two ways: (1) `CalendarDecorationContent` now also refetches on `scenePhase` becoming `.active` (`.onChange(of: scenePhase)`), covering "leave the app to edit Calendar, come back"; (2) `CalendarService` now observes `EKEventStoreChanged` via `NotificationCenter` and refetches automatically, covering edits made while MyTablo stays open/foregrounded (e.g. via Siri). Full project builds clean, 0 new warnings.
  - **Confirmed 2026-08-08 (later same day) on Adele's real device — the multi-calendar refetch fix works.** Adele added events to a second (different) calendar account and the decoration picked them up correctly, showing 2 events. She deliberately doesn't want a full calendar view crammed onto the small decoration ("I don't think people need to have like the whole calendar on there... such a cute icon") — current 3-event-cap card stays as the intended design, not a limitation to fix. A fuller/expandable calendar view noted as a possible future idea only, not requested.

**Photo decorations — built 2026-08-08, not yet tested on device.** Adele confirmed "will test" and asked for this to be built for the next testing round. Implemented per the v1 scope already logged below (photo picked from the library, placed inside Kate's Polaroid frame artwork, drag/resize/rotate/delete like any other decoration):
- Found Kate's frame artwork at `~/Documents/Curio Design Assets/Stickers/Polaroid Cart/PolaroidTransparent.PNG` — a Polaroid card with a genuinely transparent (alpha-0) photo window, confirmed via pixel inspection, so a user's photo can be composited *behind* it and the frame drawn on top. Added to the project as a new `Decoration-PolaroidFrame` imageset (this project uses Xcode's file-system-synchronized groups, so no `project.pbxproj` edit was needed — new assets are picked up automatically, same as prior sticker-pack additions).
- `Decoration` (`Models/Decoration.swift`) gained an optional `photoImageData: Data?` field (default nil, so existing saved decorations stay loadable) and an `isPhotoFrame` flag keyed off a sentinel `imageName` (`Decoration.photoFrameImageName`), mirroring the existing sticky-note pattern exactly.
- `DecorationView.swift` renders photo frames by measuring the transparent window's bounding box as a fraction of the frame artwork (computed via a flood-fill from the image center: window spans roughly x 8–92%, y 8–79% of the frame), then layering the user's photo (`.scaledToFill()`, clipped to that window) behind the frame image — so it drags/resizes/rotates/deletes/z-orders exactly like every other decoration, including inside shared snapshots, with no extra plumbing needed there (same reuse story as sticky notes).
- Added a "Add Photo" button (`photo.on.rectangle.angled` icon) to Style view's control bar (`CoffeeTableViewIntegrated.swift`), opening a system `.photosPicker`; a pencil-icon button (reusing the same overlay slot as the sticky-note edit pencil, now shared between both decoration types) lets you replace an existing photo frame's image. Both paths route through the same `normalizedOrientation()` JPEG pipeline already used for book covers (widened from `private` to `internal` in `LibraryView.swift` so this file can reuse it) — kept deliberately to just the gallery picker for v1, not camera/scan, to avoid re-touching the camera-capture flow's fragile history logged elsewhere in this file.
- `CoffeeTableCompositionViewModel` got matching `addPhotoDecoration(imageData:)` / `updatePhotoDecorationImage(_:imageData:)` methods alongside the existing decoration ones.
- **Share-time privacy reminder, added per the open scope item below:** `TabloView.swift` now shows a small caption ("Sharing includes any personal photos visible on your table") above the control bar whenever the current composition contains at least one photo decoration — a passive on-screen notice rather than a blocking confirmation, to avoid re-touching `ShareLink`'s presentation flow (which has its own fraught fix history in this file). Confirmed 2026-08-08: trigger is keyed off `isPhotoFrame` specifically (`hasPhotoDecorations` in `TabloView.swift`), so sticky notes alone do not trigger it — this was already correct, no change needed. **Future scope (per GG's review):** broaden the trigger to other genuinely sensitive live object types once they exist — calendar events, health data, precise location — not just photos.
- Full project builds clean, 1 pre-existing unrelated warning (`TabloView.swift`, `UIScreen.main` deprecation). **Not yet tested on device** — needs a real-device pass: add a photo decoration, confirm it drags/resizes/rotates/deletes correctly, replace its photo via the pencil button, and confirm it appears (with the privacy caption visible) in a shared snapshot.
Later/Kate-supplied variants (not v1): torn-edge print, tiny framed portrait, photo-booth strip, photo tucked into an open journal page. Guiding question for what belongs in this whole "app within an app" section: "what would someone lovingly place on their real table?"

**"Open journal page" clarified 2026-08-08.** Kate's idea, via Adele: if she supplies open-journal-page artwork, could the app let a user add text on top of it in Style mode, or — bigger ask — actually draw on it freehand with Apple Pencil (or a finger)? Freehand drawing is genuinely feasible: Apple's **PencilKit** framework (`PKCanvasView`) natively supports both Apple Pencil and finger input on iPhone/iPad, so a "draw directly on your table" canvas layer is realistic future scope, not just a nice-to-have wish. Adele also flagged this as a good candidate for the widget vision and noted iOS 27's rumored foldable-phone support as a reason full-screen finger drawing matters. Not scoped or started — sticky notes' text-entry work (next up) is the more basic version of "add text to the tableau"; freehand drawing would be a separate, larger PencilKit-based feature on top of that.
- **Future idea (2026-08-08) — draw on sticky notes.** Adele asked, after Sticky Notes shipped, whether notes could eventually support freehand drawing/doodles instead of just typed text. Same PencilKit approach as the open-journal-page idea above applies here too — a small `PKCanvasView` per note instead of (or alongside) the `TextEditor` in `StickyNoteEditorView`, with the drawing serialized and stored on the `Decoration`. Deliberately deferred, not scoped — flagged as future scope so it isn't lost, not a commitment.

**Big vision re-raised 2026-08-08 — "harness what the phone already does" / personal dashboard direction.** Adele's framing: right now she juggles several different widgets from different apps on her home screen just to approximate a personal dashboard, and wants MyTablo to *be* that dashboard instead — load it once, leave it open/pinned, and get everything from one Tableau. Two people, two different flavors of the same idea (both fold into this section, not separate features):
- **Kate's flavor (journal/paper person):** the Polaroid photo-drag-in and blank writable paper/journal ideas above — decorations you personalize with your own content.
- **Adele's flavor (an actual glanceable OS-level dashboard):** specific asks this session, each mapped to the Apple framework that would drive it —
  - **Drag your own photos onto the blank Polaroid artwork Kate is providing** — same underlying need as "let a user add their own photo into a blank instant-photo frame," already logged above; this is Adele independently confirming it, from the opposite persona. Likely reuses the existing custom-cover photo picker pattern (`PhotosPicker`/camera), just targeting a decoration instead of a book cover.
  - **Steps widget** (HealthKit, read-only step count) and **water tracker** (HealthKit has a water-intake sample type, though most water-tracking is done by third-party apps outside HealthKit, so this may need its own simple in-app tracker rather than reading from Health).
  - **Tap a "currently listening" decoration to jump straight into the user's audiobook app** — feasible as a deep link (e.g. opening Apple Books/Audiobooks via URL scheme) rather than in-app playback; showing *which* audiobook is playing without the user manually setting it isn't something Apple exposes an API for, so this would likely start as a manual "what are you listening to" pick, not automatic detection.
  - **Location decoration**, **clock/time display** (clock now actually built, see "Weather decoration" section above — added as a diagnostic, so this idea has moved from future-list to shipped), **favorite photos of loved ones** — all already covered above (Core Location map/globe idea, decorative clock idea, and the same photo-decoration mechanism as the Polaroid idea).
  - **Alarm decoration** — Adele explicitly said it's fine if this is decorative-only and doesn't actually integrate with the real Alarms app, consistent with the "no public API for that" limitation already noted above.
  - **Adele is pre-emptively fine with the permission prompts** all of this implies (HealthKit, Photos, Location, Calendar, etc.) — no pushback to plan around there.
- **New ideas raised 2026-08-08 (later session, after Clock shipped):**
  - **Camera-launcher decoration** — a table object that, when tapped, opens the phone's actual system Camera app (forward/back camera), not an in-app capture flow. **Not buildable as literally described, explained to Adele, not built**: Apple does not publish a URL scheme or other public API for third-party apps to deep-link into the stock Camera app (unlike Settings, Mail, Maps, etc., which do have documented schemes) — same class of limitation already noted for the Apple Watch face and real Alarms app integration ideas. Realistic alternative if revisited: a decoration that opens *this app's own* camera capture (reusing the existing `CameraCoverCapture.swift` flow) styled to look like a camera object — genuinely buildable, just not literally "launches the iPhone's Camera app." Adele accepted the explanation, not requested further.
  - [x] **Calculator decoration — built 2026-08-08, builds clean, not yet tested on device.** `Decoration.calculatorImageName`/`isCalculator` (`Models/Decoration.swift`). On the table it shows as a small static icon card (`CalculatorDecorationContent`, `DecorationView.swift`); selecting it reveals an "open" button (reused overlay slot alongside the existing sticky-note/photo edit pencil, branches by decoration type) which presents a full working `CalculatorSheetView` — standard +/−/×/÷, %, ±, clear, decimal point, chained operations. Pure SwiftUI, no permissions or entitlements needed. A `divide.square.fill` button in Style view's control bar adds one.
  - [x] **Location decoration — built 2026-08-08, builds clean, not yet tested on device.** Mirrors the Weather decoration pattern: `Decoration.locationImageName`/`isLocation`, new `Services/LocationService.swift` (`CLLocationManager` when-in-use request + `CLGeocoder` reverse-geocoding down to city/region name — no map/globe visual yet, just a pin icon + place name card, per the original "little map/globe" future idea logged earlier), `LocationDecorationContent` in `DecorationView.swift`, `mappin.circle.fill` control-bar button. Reuses the same `NSLocationWhenInUseUsageDescription` permission Weather already added (broadened the description text in `project.pbxproj` to cover both). No new entitlement needed (unlike Weather/WeatherKit). New warning: `CLGeocoder`/`reverseGeocodeLocation` deprecated in iOS 26 in favor of MapKit's `MKReverseGeocodingRequest` — left as-is for now (same tolerance as the pre-existing `UIScreen.main` warning), functionally fine, worth migrating later if MapKit's replacement API proves stable.
  - [x] **Music decoration — rebuilt 2026-08-08 (later session) as a real bundled-audio record player, per Adele/Kate's explicit request ("make us a little record player... like the MD vinyl"). Builds clean (0 new warnings), not yet visually confirmed — see tooling note.** This supersedes the earlier deep-link-only version described below. Adele picked option (3) from the prior session's steer list — real audio files, sourced from Pixabay Music (per [[project_music_decoration_pixabay]]) — plus the visual/interaction direction from this same conversation (turntable object, per-track "little album" artwork, tap-to-open full player rather than table-mode-direct controls, to stay within the proven Calculator/Weather interaction pattern rather than touching the flagged-fragile gesture-layering code).
    - **5 tracks downloaded from Pixabay Music** (royalty-free, no login required from each track's own detail page — the search-results-page download button needs an account, the per-track page's "Free download" button doesn't) and bundled at `ReadingTable/Resources/Music/record-track-1.mp3` through `record-track-5.mp3` (~4-4.7MB each, ~21.6MB total): "Calm Peaceful Chill Hop" (FASSounds), "Lofi Study Session" (alex-morgan), "Lofi Study" (The_Mountain), "Lofi Study Rainy Night" (alex-morgan), "Study Lofi Music" (mirostar). `ReadingTable/` is a file-system-synchronized Xcode group, so dropping files into it bundles them automatically — no `project.pbxproj` edit needed, same as prior sticker-pack additions.
    - **New `Services/RecordPlayerService.swift`** (`@MainActor`, `ObservableObject`, singleton `.shared` like Weather/Calendar): wraps `AVAudioPlayer`, one `RecordTrack` struct per bundled file (title, artist, a distinct accent color per track — this *is* the "little albums" changing-artwork effect Adele asked for, procedural rather than real cover art since these are stock tracks with no album art of their own), `play()`/`pause()`/`togglePlayPause()`/`skipToNext()`/`skipToPrevious()`, each track loops (`numberOfLoops = -1`) for ambient background listening.
    - **Table object redesigned** (`DecorationView.swift`): replaced the old flat white "Music" icon tile with `RecordPlayerDecorationContent` — a small portable-turntable casing (same dark-plastic-and-brass material family as `DigitalClockFace`/`TravelClockFace`/`CalculatorDecorationContent`, per the design review's "shared lighting angle... apparent scale for all objects" rule) with a spinning vinyl disc (`VinylDiscView`, colored label per current track) and a brass tonearm that lifts/drops with play state. Spin angle is derived from a periodic `TimelineView` rather than a started/stopped SwiftUI animation — pausing just stops requesting new frames, so the disc freezes cleanly at whatever angle it was already at with no snap-back glitch (same technique as the existing digital clock's live-updating `TimelineView`).
    - **New `RecordPlayerSheetView`**: tapping the table object (via the existing "open" overlay button, same slot/pattern already proven for Calculator) opens a full dark-themed player — a larger spinning vinyl (`BigVinylDisc`) with the current track's title/artist, play/pause, previous/next ("flip the record" — this doubles as Adele's alternate "turning the radio station over" idea, since skipping visibly swaps to a new colored record), and a secondary "Open in Apple Music" link (per DESIGN_REVIEW_LIVE_OBJECTS.md: "Opening Apple Music can remain a secondary action").
    - Re-enabled the Music "add" button (`music.note` icon) in Style view's control bar (`CoffeeTableViewIntegrated.swift`), which had been hidden since the design review — same precedent as Clock/Weather being re-enabled once redesigned.
    - **Interaction scope, deliberately kept small per this session's decision**: play/pause/skip only work from inside the expanded sheet, not directly on the small tabletop object — matches how Calculator already works, and avoids touching the drag/tap gesture-layering code flagged elsewhere in this file as a repeated source of regressions (Group-modifier propagation, `highPriorityGesture` blocking nested buttons). Full table-mode-direct controls (per the design doc's ideal) remains a possible future pass, not started.
    - **Tooling note**: this session's Simulator reproduced the same synthetic-tap-not-registering issue logged repeatedly elsewhere in this file (background `onTapGesture` to reveal the control bar didn't respond either, at multiple valid on-screen coordinates) — build succeeded and installed cleanly, but visual confirmation of the new turntable/player wasn't possible this session. **Needs a real-device retest**: add a Music decoration, confirm the turntable renders correctly with a spinning disc and brass tonearm, tap it open to the full player, confirm audio actually plays/loops, and confirm Next/Previous visibly swaps to a differently-colored "record."
    - Old deep-link-only version, kept here for history: shipped earlier the same day as `Decoration.musicImageName`/`isMusic` with a static white "Music" tile and a button that opened `https://music.apple.com` via universal link. That plain launcher button/behavior is now gone, replaced by the record player above; the Apple Music link survives only as the sheet's secondary action.
- Treat this as the connective narrative for the whole "app within an app" section: the individual pieces (sticky notes, calendar, clock, weather, music, journal/paper, Polaroid photos, steps/water/audiobook/location, and now camera-launcher/calculator) are all facets of one destination — a phone dashboard styled as a coffee table — rather than a pile of unrelated small features. Still not scoped or started beyond the already-confirmed order (Sticky Notes, then Calendar, then the just-shipped diagnostic Clock) — this is direction-setting for what comes after that.

**Reading-continuity framing, via Adele's ChatGPT ("GG") 2026-08-08 — not scoped or started, positioning notes for later.** GG's take on the app's competitive moat: no single feature here (calendar decoration, Polaroid photos, etc.) is hard to copy in isolation — the moat is that MyTablo bundles a real book library, Kate's visual identity, easy arranging/sharing, personal content, and live data into one coherent, personal experience, which is much harder to replicate wholesale. For "reading continuity" specifically, GG suggested three tiers: (1) track reading state directly in-app — extend the existing Currently Reading flag with progress %, last-read date, favorite passages, a reading goal; (2) a book/audiobook decoration that deep-links out to wherever the user actually reads it (Kindle, Audible, Apple Books); (3) automatic position sync from those services — GG checked and found no public API for this (Amazon doesn't expose Kindle/Audible reading position to third-party apps; Audible's Alexa Voice Service integration route requires Amazon review with strict limits), so tier 3 isn't buildable today. Recommended near-term framing: a book showing "67% read" that opens the right reading app, with the percentage updated by hand for now, architected so real sync could slot in later if Amazon/others ever expose one. Fits directly into the "personal dashboard" narrative above rather than being a separate idea.

### Future direction — Widgets: Tablescape Mode + Library Shelf Mode (logged 2026-08-07)
**Do not build any of this now — architectural/product direction only, retained for a future dedicated widget effort. Explicitly not part of the current visual-rendering patch.**

Two complementary widget presentation modes are planned:

1. **Tablescape Mode** — a responsive rendering of the user's saved Tablo tabletop and objects (the existing composition view, adapted for widget sizes).
2. **Library Shelf Mode** — a simpler, compact presentation showing the spines of books in the user's library, inspired by the pleasure of viewing a physical bookshelf. Especially important for smaller widget sizes where a full tablescape would be too detailed to read.

Requirements for Library Shelf Mode, once built:
- Generate book-spine representations from each book's stored cover colors, title, and author — no separate spine photography required.
- Preserve enough individuality per spine that the shelf feels like the user's real collection, not a generic placeholder.
- Support small, medium, large, and extra-large widget families wherever Apple makes those sizes available.
- Small widget: only a selected handful of spines.
- Larger widgets: multiple shelves or more of the collection.
- Possible collections to show: Currently Reading, Recently Added, Favourites, or a user-selected shelf.
- Tapping the shelf opens Tablo's Library.
- Use only interactions and refresh behavior officially supported by WidgetKit — no unsupported/custom interaction hacks.
- No conventional controls, tiny unreadable text, or excessive detail — this should read as a beautifully photographed miniature bookshelf, not a database list.
- Lock Screen and StandBy variants should be deliberately simplified designs, not a shrunk-down version of the full bookshelf pushed past legibility.

This builds on the already-logged "Exploration: lock screen / widget support" item above — this is the detailed spec for that exploration, not a separate request.

### Future idea — separate "vision board" app (logged 2026-08-07)
User wants a second, similar app built on the same pattern as ReadingTable but for a vision board instead of a coffee table of books. Kate says she already has the artwork ready for it. Not started, not scoped — would be a new Xcode project, not a feature added to ReadingTable itself.
- Camera and barcode-scanning features need a physical device (cable-connected via Xcode) — the iOS Simulator has no real camera feed.
- There are also 5 unused, unrelated `table-*.imageset` assets already sitting in Assets.xcassets (concrete/linen/marble/travertine/wood, no "Kate" prefix) — orphaned placeholders predating Kate's photos, not wired into any UI. Safe to delete if cleaning up, but left alone unless asked.

## iPad real-device test round (logged 2026-08-08, later session) — handoff notes
Adele tested on her iPad (Simulator taps have historically been unreliable per the tooling notes above, so real-device testing is the primary signal). All fixes below build clean (`xcodebuild`, iphonesimulator, 0 errors, 1 pre-existing unrelated warning — `TabloView.swift`, iOS 26 `UIScreen.main` deprecation). **None of the fixes in this section have been re-tested on device yet** — that's the next step for whoever picks this up.

- [x] **Bug found: sharing from iPad rendered the book massive and brought back the black bar at the bottom of the shared image.** Root cause: `TableSnapshotView`'s `size` parameter defaulted to a hardcoded iPhone-shaped canvas (402×874), and `TabloView.refreshShareImage()` never overrode it — so on iPad, book/decoration positions (stored as absolute coordinates tied to the *actual* on-screen table size) got rendered onto a canvas shaped for a completely different device, and the `scaleEffect(1.04, anchor: .bottom)` crop math (tuned for that one fixed aspect ratio) fell short again on the mismatched iPad aspect. Fixed by capturing the live table's `GeometryReader` size into a new `tableSize` state in `TabloView.swift` and passing it into `TableSnapshotView(composition:size:)` on every `refreshShareImage()` call, so the exported snapshot always matches whatever device it's running on. **Needs iPad retest**: share a table with a book on it, confirm the book is correctly sized and there's no black line at the bottom.
  - **Follow-up, unresolved**: Adele then sent a screenshot showing the shared image letterboxed with large black bars above *and* below the content (not just a thin line). Asked for clarification on which app/screen that was (Messages vs. the Share Sheet preview vs. Photos) to know whether it's something in our PNG or just how that viewer displays a differently-shaped image — she didn't confirm and the conversation moved on to a different bug instead. **Unresolved and not investigated further** — if it recurs, get a screenshot with the surrounding app chrome visible (not just the image) to tell whether it's baked into the exported PNG or is view-chrome from whatever app displayed it.
- [x] **Bug found: iPad share sheet showed a redundant "MyTablo" line above the share caption.** `ShareLink`'s `preview: SharePreview("MyTablo", image: ...)` (`TabloView.swift`) supplied a separate title text that showed up alongside the `message:` caption ("Here's what's on MyTablo right now 📚✨"), reading as duplicated/cluttered. Fixed by passing an empty title (`SharePreview("", image: ...)`) so only the intended caption shows. **Needs iPad retest.**
- [x] **Bug found: background-removal cutouts (Take Photo / Choose from Library) came out slightly skewed/off-kilter** — reproduced live on a photo of "Psycho-Cybernetics." Root cause: unlike **Scan Cover** (which uses VisionKit's live document scanner to detect the book's 4 corners and correct perspective before saving), Take Photo and Choose from Library never straightened the photo at all — "Remove Background" (`VNGenerateForegroundInstanceMaskRequest`) just cuts around the subject's silhouette exactly as photographed, tilt included, with no way to tidy it up afterward. Fixed by adding `straightenedIfPossible(_:)` (`CameraCoverCapture.swift`) — uses `VNDetectRectanglesRequest` to find the book's corners and Core Image's `perspectiveCorrection` filter to square them up — run automatically in `BackgroundRemovalReviewScreen` via `.task` as soon as the photo arrives, before either "Remove Background" or "Use Photo" (as-is) can act on it. Silently falls back to the original photo if no confident rectangle is found (best-effort touch-up, not a user-facing action, so no new button or error state). **Needs iPad retest**: photograph a book at a slight angle via Take Photo, confirm it comes out straight both with and without Remove Background applied.
- [x] **Feature: "Currently Reading" flag, so the widget shows a chosen book instead of just the most recent one** — the concrete need behind the widget's placeholder "most recently added" behavior logged above. Added `isCurrentlyReading: Bool = false` to `Book` (`Models/Book.swift`, defaults so existing local records stay loadable). `LibraryViewModel.setCurrentlyReading(_:isCurrentlyReading:)` toggles it, clearing the flag on any other book first — only one book can be "currently reading" at a time since the widget has one slot. `BookTileView` (`LibraryView.swift`) got a bookmark-icon button (top-left of the cover) to toggle it, filled/accent-colored when active. `ReadingTableWidgets.swift`'s `TimelineProvider` now fetches the flagged book first, falling back to most-recently-added if none is flagged (so the widget is never empty). **Needs iPad retest**: mark a book as currently reading, confirm the widget picks it up (also see the still-open "widget not loading books on iPad" bug below — worth checking whether this fix incidentally affects it).
- Adele reported separately, **not yet investigated**: the home screen widget on iPad shows up but isn't loading any books at all (stays empty) — distinct from the "wrong book" placeholder behavior above. Next step for a future session.
- Adele also asked (2026-08-08) about adding **book categories/tags** (e.g. back-to-school vs. lifestyle) with possible future smart sorting — discussed as a separate, larger feature (needs a tagging UI + Library filtering) and intentionally deferred in favor of the smaller Currently Reading flag above. Not scoped or started.
- Also discussed: a request for an **Apple Watch face "mirror"** decoration — flagged to Adele that Apple doesn't expose a way for an app to read/mirror actual watch face content, so true mirroring isn't possible; a decorative clock-face object (already on the future-ideas list above) or a proper watchOS companion app are the realistic alternatives. Not scoped or started.
- [ ] **Reported 2026-08-08 — iPad portrait: "not quite the right canvas size."** Adele flagged the table canvas looks slightly off on iPad in portrait orientation but didn't specify exactly what looked wrong (stretched, squished, cut off at an edge, wrong aspect ratio, etc.). Checked the code: the surface background image already uses a `GeometryReader`-sized `.scaledToFill()` + `.clipped()` (`CoffeeTableViewIntegrated.swift`), so it should always fill the screen at any device size — but book/decoration *positions* are stored as absolute x/y coordinates tied to whatever screen size they were placed on (the same root cause behind the iPad share-sizing bug fixed above), so items arranged on one device/orientation could sit slightly wrong-feeling on another. Not root-caused yet — needs a follow-up from Adele describing/screenshotting specifically what looks off (e.g. compare portrait vs. landscape, or note if it's the background surface itself vs. where books land) before this can be fixed with confidence.

## Requested 2026-08-08 (from iPad session) — added to backlog
- [ ] **More sticker packs.** Adele asked to keep adding Kate's remaining packs — she specifically mentioned Kate has a "paper" pack plus other bits ready. Cross-check against the not-yet-added list already logged above (Desk Supplies, Pastels Washi Tape, Witchy Desk Supplies, Ultimate Crystal Collection, Fall Oracle Deck) — "paper" doesn't obviously match any of those names, so check `~/Documents/Curio Design Assets/Stickers/` for a pack Adele/Kate may not have named exactly yet.
- [x] **"Favourite"/widget-loading feature — already built, just needs the pending iPad retest.** This is the "Currently Reading" flag logged just above (`isCurrentlyReading` on `Book`, bookmark toggle in `BookTileView`, widget prefers the flagged book) — same feature Adele is asking about here under a different name. Nothing new to build; next step is still the iPad retest already queued.
- [ ] **Other widget sizes (medium/large/extra-large).** Only the small (2×2) "Current Read" widget exists today (`ReadingTable/Views/Widgets/SmallWidgetView.swift`, `ReadingTableWidgets/ReadingTableWidgets.swift`). Adding medium/large families ties into the already-planned Tablescape Mode / Library Shelf Mode widget direction documented in detail further below — that's the natural place to design what larger sizes actually show (more spines, a mini tablescape, etc.) rather than just stretching the small layout.
- **Priority note (2026-08-08, updated same day) — Adele wants Weather moved up next.** Originally Weather was ranked after Calendar/Clock in the "app within an app" priority list below ("higher effort, needs Apple Developer entitlement setup"). Adele has now said she'd like to prioritize Weather sooner ("such a cool unique feature"), ahead of Clock/Location.

- [~] **Weather decoration — built 2026-08-08, code complete and builds clean, but blocked on one manual Apple Developer Portal step before it can actually run.** Mirrors the Calendar decoration pattern exactly (live-fetched, nothing stored on the `Decoration` itself): `Decoration.weatherImageName` sentinel + `isWeather` flag (`Models/Decoration.swift`), new `Services/WeatherService.swift` (`@MainActor`, wraps `CLLocationManager` + WeatherKit's `WeatherService.shared.weather(for:)`, requests when-in-use location, refetches on `scenePhase` becoming active same as Calendar), a `WeatherDecorationContent` card (icon + temperature + condition text) in `DecorationView.swift`, and a "cloud.sun.fill" button in Style view's control bar (`CoffeeTableViewIntegrated.swift`) to add one. Added `INFOPLIST_KEY_NSLocationWhenInUseUsageDescription` to both Debug/Release configs (`project.pbxproj`) and `com.apple.developer.weatherkit` to `ReadingTable.entitlements`. Full project builds clean via `xcodebuild` (iphonesimulator), 2 pre-existing unrelated warnings only (`CalendarService.swift` Swift 6 concurrency note, `TabloView.swift` `UIScreen.main` deprecation) — no new warnings.
  - **Blocker, needs Adele: WeatherKit is a paid-tier Apple capability, unlike Calendar/EventKit which needed no entitlement.** Manually adding the entitlement (done above) only gets the code to compile — actually calling the weather API at runtime will fail with an authorization error until the WeatherKit capability is enabled for this app's App ID in the Apple Developer Portal, which requires a **paid** Apple Developer Program membership (not the free tier). The safest way to do this is in Xcode itself, not by hand-editing files further: open the project → select the "ReadingTable" target → **Signing & Capabilities** → **+ Capability** → add **WeatherKit**. With automatic signing and a paid account, Xcode registers the capability with Apple and syncs the entitlement automatically. **Next step is Adele doing that one-time Xcode step, then a real-device retest** (Simulator can compile and may even run, but WeatherKit calls are more reliably tested on a real device with a genuine provisioning profile).
  - Deliberately left out of `TabloView.swift`'s `sharingPrivacyNotice` — a temperature/condition reading doesn't disclose anything as identifying as a calendar event title or a personal photo, so it wasn't added to the photo/calendar privacy caption trigger. Worth revisiting only if that assumption gets pushback.
  - **Adele confirmed 2026-08-08 her Apple Developer account is paid ($99/yr) and WeatherKit already shows as added in Xcode's Signing & Capabilities.** Rebuilt clean (`xcodebuild`, iphonesimulator) — build log confirms `com.apple.developer.weatherkit = 1` is now actually embedded in the compiled entitlements, so the capability is wired through correctly on the code side. Tried to click-test it live in Simulator (launched the app, set a simulated location) but hit the **same synthetic-input tooling issue logged earlier today** — this time nothing responded at all, not just `Button`s (a plain drag on a decoration and the tap-to-reveal-control-bar gesture, both previously confirmed working in isolation, didn't register either). Also worth knowing independent of that: even a clean Simulator build only ad-hoc-signs (`codesign --sign -`, no real team/provisioning check), so a Simulator pass alone wouldn't fully prove Apple's servers authorize this app for WeatherKit anyway — a real-device test is the meaningful confirmation here, more so than for previous decorations. **Needs Adele to add the weather decoration on her own device/Simulator and confirm it shows a real temperature, not just that it compiles.**
  - **Still not loading as of 2026-08-08 (later same day) — weather decoration reported still failing on Adele's real device**, no further detail yet on what "not loading" looks like (blank card, stuck spinner, "Enable Location access" placeholder, or something else). **Diagnostic added, not a fix for weather itself: built a Clock decoration** (`Decoration.clockImageName`/`isClock` in `Models/Decoration.swift`; `ClockDecorationContent` in `DecorationView.swift`, a `TimelineView(.periodic(from:by:))` showing live time + date, updating every second; a `clock.fill` button in Style view's control bar, `CoffeeTableViewIntegrated.swift`) — chosen specifically because it needs **zero permissions or entitlements**, unlike Calendar (EventKit) or Weather (WeatherKit + paid-tier capability). The idea: if Clock also fails to render/update on Adele's device, that points to something broader (e.g. the decoration-rendering pattern itself, or the same tooling/input issue logged elsewhere) rather than something WeatherKit-specific; if Clock works fine, that narrows the weather bug specifically to WeatherKit's authorization/network path. Full project builds clean via `xcodebuild` (iphonesimulator), 1 pre-existing unrelated warning only (`TabloView.swift`, `UIScreen.main` deprecation) — no new warnings. **Not yet tested on device** — next step is Adele adding a Clock decoration and confirming whether it shows/updates correctly, which will help isolate the weather bug.
  - **Design review 2026-08-08 (GG/Codex, via `DESIGN_REVIEW_LIVE_OBJECTS.md`) — the plain white-tile look for Clock/Calculator/Weather/Music was rejected by Adele and Kate.** Verdict: these read as generic app icons, not tabletop objects; redesign the object metaphor before polishing further. Doc mandates building one exemplar (recommended: Clock, since it needs no permissions) and getting visual approval before adapting the pattern to the other three. Immediate response, this session:
    - **Hid the Weather, Calculator, and Music "add" buttons from Style view's control bar** (`CoffeeTableViewIntegrated.swift`) per the doc's immediate recommendation — any already-placed decorations of these types still render and work exactly as before, only new ones can't be added from the picker until each gets its own redesign.
    - **Rebuilt the Clock decoration as the exemplar** (`DecorationView.swift`): replaced the flat white-square `ClockDecorationContent`/`AnalogClockFace` with two properly art-directed objects — `DigitalClockFace` (dark plastic bedside-clock casing, amber glowing LED-style digits, small feet) and `TravelClockFace` (brass bezel with an angular-gradient sheen, cream dial, carrying ring, slim hands). Both are hand-drawn SwiftUI vector art standing in for real Kate/Gigi artwork, which doesn't exist yet — the code is structured so a real casing image can later slot in behind the same live-overlay logic with minimal change. Also fixed the exact truncated-minutes bug the doc called out (`18:3` instead of `18:03`) by switching the digital face's format to `.dateTime.hour().minute(.twoDigits)`. The existing pencil-icon style-toggle button (analog/digital) still works unchanged. Kept the Clock "add" button visible (unlike Weather/Calculator/Music) so Adele/Kate can actually see and approve the new design.
    - Full project builds clean (`xcodebuild`, iphonesimulator), 0 new warnings (only the 2 pre-existing unrelated ones). **Not yet visually confirmed** — this session's Simulator reproduced the same synthetic-tap-on-`Button` limitation logged elsewhere in this file (the background "reveal control bar" `onTapGesture` worked, but tapping the "Style" `Button` itself did not register), blocking a click-through screenshot of the new clock. Needs a real-device retest: open Style view, add a Clock, and confirm with Adele/Kate whether the new travel-clock/digital-clock look clears the design-review bar before touching Calculator, Weather, or Music.
    - **Not yet done, deliberately deferred to keep this change small and low-risk**: the doc's interaction rule that live objects should be usable from normal Table mode without entering Style mode. Right now Table mode renders all decorations with `isInteractive: false` and a full-screen tap-to-collapse layer sits on top of them, so nothing on the table (clock included) is actually tappable outside Style mode yet — confirmed by reading `TabloView.swift`. Fixing this touches the same gesture-layering code that has caused several past regressions in this file (Group-modifier propagation, `highPriorityGesture` blocking nested buttons), so it's being held for its own careful, separately-tested pass rather than bundled in here.
  - **Clock exemplar approved by Adele 2026-08-08 ("Love the clocks. They're absolutely fabulous.")** — clears the doc's gate to adapt the pattern to Weather next (Calculator/Music remain hidden/deferred). Adele had also separately asked to bump Weather ahead of Calculator/Music in priority, so it's the natural next pick.
  - **Weather — root-caused, fixed, and redesigned as the second live-object exemplar 2026-08-08, not yet tested on device.** Per the design doc's instruction to diagnose WeatherKit independently of the redesign:
    - **Root cause of "not loading" found**: `WeatherService.swift`'s `CLLocationManagerDelegate.didFailWithError` was a silent no-op ("leave the last-known state on screen"). Since the service's *initial* state is also its blank-looking state (`temperature = "--°"`, `condition = ""`), any location fetch failure — a slow/no GPS fix, airplane mode, a denied one-off prompt, anything — left the decoration stuck at that same blank default forever with zero feedback, indistinguishable from "still loading" or "broken." Fixed by adding `@Published var isLoading`/`fetchFailed` state, set properly in `requestLocationAndFetchWeather()`, `fetchWeather(for:)`'s catch block, and now a real `didFailWithError` handler instead of the no-op.
    - **Redesigned as a brass desk barometer** (`WeatherDecorationContent`, `DecorationView.swift`), reusing `TravelClockFace`'s exact material palette (brass angular-gradient bezel, cream dial, ink-colored ticks) so it reads as part of the same object family per the doc's "shared lighting angle... apparent scale for all objects" requirement, plus two small brass feet to ground it as a tabletop instrument rather than a wall-hung piece. The three failure/loading states are now styled into the dial face itself instead of a separate blank white card — a location-denied state, a "Reading the sky…" loading state, and an explicit "Weather unavailable" state for genuine fetch failures — directly addressing the doc's complaint that "the failure state should be intentional and helpful... not a broken-looking empty object."
    - **Re-enabled the Weather "add" button** in Style view's control bar (`CoffeeTableViewIntegrated.swift`), following the same precedent as Clock, so Adele/Kate can see and approve it on a real device. Calculator and Music stay hidden.
    - Full project builds clean (`xcodebuild`, iphonesimulator), 0 errors.
    - **Visual design confirmed 2026-08-08 by Adele and Kate on iPad.** Kate was asked live whether the barometer should be silver (to differentiate from the gold clock) or match the clock's brass — decided to keep it matching the clock ("Keep it as it is. Uh, love it."). No code change needed, the barometer already uses the same brass palette as `TravelClockFace`.
    - **Still not loading real weather data as of this same iPad test, but narrowed down: stuck on the "Reading the sky…" loading state, never resolving to either a temperature or a failure message.** Root cause: `CLLocationManager.requestLocation()` has no built-in timeout — if the device can't get a location fix at all, neither `didUpdateLocations` nor `didFailWithError` ever fires, so the code just waits forever. Most likely trigger on an iPad specifically: many iPads are Wi-Fi-only with no GPS chip, so indoor location fixes rely on Wi-Fi-network triangulation, which can be slow or simply unavailable depending on the network. Fixed by adding `startTimeoutWatch()` (`WeatherService.swift`) — after 15 seconds with no result, it surfaces the same honest "Weather unavailable" state used for a real fetch failure, instead of hanging on the loading text indefinitely. This does not fix WeatherKit/location itself (still worth testing outdoors or near a window to see if it ever succeeds), it just guarantees the object never looks silently stuck. Full project builds clean, 0 errors. **Not yet re-tested on device.**

## Utility-object design reset — full handover 2026-08-08 (evening), from Adele/Kate/Gigi review of screenshots

**Verdict: Calendar is the only new utility object whose visual direction currently feels right.** Record player, Clock, Weather, and Calculator are all now **prototypes, not approved designs** — flat, childish, clip-art-like next to the app's photorealistic books/flowers/Polaroids. Would rather temporarily remove an object than ship it below MyTablo's quality bar. **No further visual implementation happened tonight** and none should happen until direction is re-approved with Adele, Kate, and Gigi in a future session.

**Preserve, don't discard:** all four objects' underlying functionality (record playback, live clock, calculator logic, weather fetch+timeout) stays as-is in code — this is an art-direction reset, not a feature rollback.

### Record player specifics
- The MD Vinyl player (referenced earlier when this was built) was a **quality reference only** — do not copy its proprietary assets or produce a simplified cartoon version of it.
- Target: a premium, photorealistic/editorial-quality transparent-background tabletop object, fully draggable/rotatable/scalable like every other object, never fixed in place, with obvious functional controls. Likely technical shape: a high-quality transparent visual asset with SwiftUI controls layered invisibly/discreetly on top. Kate may supply artwork, or Gigi may help generate/prepare it.
- The 5 bundled tracks are royalty-free audio from Pixabay (see [[project_music_decoration_pixabay]]) — **remove every Apple Music reference and the "Open in Apple Music" link/branding**, and instead accurately label the audio as bundled royalty-free music with correct artist/attribution per licence. Playback (play/pause/skip) must work from normal Table mode, not just inside the Style-mode sheet — this reverses the "interaction scope deliberately kept small" decision logged in the original Music build above. Show a clear playing-state indicator (record rotation, tonearm movement, or similar).

### Clock / Weather / Calculator specifics
Pause current artwork (`DigitalClockFace`, `TravelClockFace`, the brass-barometer `WeatherDecorationContent`, and the plain calculator tile) — functional logic can stay, but none of it is approved artwork. Standing design bar for every future utility object:
- Must look deliberately placed on a beautiful coffee table — no generic cards, white squares, SF Symbol tiles, or cartoon drawings.
- Must share the depth/lighting/material quality/restraint of existing books and decorations.
- Avoid cramming in too much info; stay readable at multiple sizes.
- **Core function must work from normal Table mode, not just Style mode** — Style mode is for adding/arranging only. (Same gesture-layering caveat as logged elsewhere in this file — Group-modifier propagation, `highPriorityGesture` blocking nested buttons — will need care when this is tackled.)
- Empty/unavailable states must still look intentional and beautiful, not broken.
- Preserve drag/scale/rotate/layer/delete on every object; meet accessibility requirements without hurting the aesthetic; never falsely claim integration with another service (ties into the Apple Music removal above).

### Next session's deliverables (before any more implementation)
1. Short audit of current implementation.
2. What functionality is retainable independent of artwork.
3. 2–3 visual-object concepts per utility (record player, clock, weather, calculator).
4. Recommendation per object: supplied artwork, generated artwork, or native SwiftUI drawing.
5. **No further implementation until this direction is approved** — this session should start with proposing/discussing, not coding.

**MyTablo acceptance test for any new/reworked object:** doesn't lower the table's visual quality; looks credible beside a real photographed book or flower; purpose obvious without explanation; core function works outside Style mode; attractive at multiple sizes; never falsely claims integration with another service; Adele and Kate would willingly leave it visible on their own tables all day.

## White Rabbits — new separate app (Team Polaris), briefed 2026-08-08, targeting ~2026-09-01 release

A **new, separate iOS app**, not a MyTablo feature. **Do not begin coding until this is scoped with Adele** — next session should start by inspecting any existing Gemini prototype/files (location not yet known to this session — ask Adele), then propose architecture and an MVP plan, preserving useful prototype work without letting it dictate the final design.

**Concept:** turns the first-of-the-month "White Rabbits" good-luck tradition into a monthly ritual app — gentle reminder to say "White Rabbits" on the 1st, confirm you remembered, unlock that month's limited-edition collectible bunny, start a fresh monthly page. Tone: simple, sleek, magical, calm, collectible, grown-up — luxury storybook/editorial stationery, explicitly **not** a cartoon habit-tracker. Never implies the app guarantees real luck — frame as a joyful personal ritual only.

**September MVP scope:**
1. Reminder the evening before and/or morning of the 1st.
2. A first-of-the-month ritual screen.
3. Simple "I said White Rabbits" confirmation.
4. One limited-edition bunny unlocked per month.
5. A monthly intention (user-entered).
6. One vision-board image per month.
7. One simple monthly habit with an understated daily tracker.
8. Journal/archive of previous months + collected bunnies.

Must handle months/time zones/notification scheduling/date changes reliably. Monthly flow: reminder → open app, see hidden bunny → confirm said "White Rabbits" → bunny reveals & joins collection → set one intention → pick one image → optionally pick one habit → month archives into the journal.

**Design/privacy constraints:** local-first where practical; no account required for MVP; optional iCloud sync is a later idea, not MVP; notification permission optional and clearly explained; accessible text/controls/contrast/reduced-motion; no dashboards, streaks, stats, or social pressure; Kate's approved bunny artwork leads the visual identity; future seasonal bunny packs may support monetization later, not at MVP's expense.

**Next session's deliverables (research/planning only, no code yet):**
1. One-page product definition.
2. September MVP vs. later roadmap.
3. Complete user journey.
4. Proposed data model.
5. Notification + first-of-month logic approach.
6. Visual direction + screen list.
7. Open questions for Adele/Kate.
8. Realistic plan for a polished September release.
